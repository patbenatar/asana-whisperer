# OpenAI API key — used for Whisper audio transcription
# Get yours at: https://platform.openai.com/api-keys
# Not required when WHISPER_API_URL points to a local server.
OPENAI_API_KEY=sk-...

# Anthropic API key — used for Claude requirements summarization
# Get yours at: https://console.anthropic.com/settings/keys
# Not required when LLM_API_URL points to a local server.
ANTHROPIC_API_KEY=sk-ant-...

# Asana Personal Access Token — used to read and update tickets
# Generate at: https://app.asana.com/0/my-apps (Personal Access Tokens section)
ASANA_ACCESS_TOKEN=...

# ── Local model overrides (optional) ──────────────────────────────────────────
# Uncomment and configure these to use local models instead of cloud APIs.
# Both can be configured independently — mix and match as needed.

# Local Whisper transcription (e.g. faster-whisper-server)
# Install: pip install faster-whisper-server
# Run:     uvx faster-whisper-server
# WHISPER_API_URL=http://localhost:8000/v1/audio/transcriptions
# WHISPER_MODEL=Systran/faster-whisper-large-v3

# Local LLM summarization (e.g. Ollama)
# Install: https://ollama.com  →  ollama pull llama3.2
# LLM_API_URL=http://localhost:11434/v1/chat/completions
# LLM_PROVIDER=openai
# LLM_MODEL=llama3.2
